const chalk = require('chalk');
const log = console.log;

log(`${chalk.red(`
You just installed a package rather than to update your lock file!`)}
${chalk.green(`Don't worry this is just a placeholder package but be aware of that for the next time.`)}
${chalk.yellow(`Dustin Deus - https://github.com/StarpTech`)}
`);